module.exports = {
  arrowParens: 'avoid',            // Avoid parentheses around a sole arrow function parameter
  bracketSameLine: true,           // Put `>` of multi-line JSX elements on the last line
  bracketSpacing: true,            // Print spaces between brackets in object literals
  singleQuote: false,              // Use double quotes instead of single quotes
  trailingComma: 'all',            // Include trailing commas where valid in ES5 (objects, arrays, etc.)
  printWidth: 120,                 // Wrap lines at 120 characters
  tabWidth: 2,                     // Set the number of spaces per indentation level
  useTabs: false,                  // Indent lines with spaces, not tabs
  semi: true,                      // Add semicolons at the end of statements
};
